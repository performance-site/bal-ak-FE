import styled from 'styled-components';

export const ListTeamContainer = styled.div`
  width: 100%;
  display: grid;
  grid-template-columns: repeat(3, 7rem);
  justify-content: space-between;
  gap: 1rem;
`;
